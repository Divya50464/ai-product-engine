spring:
  application:
    name: ai-product-engine

  ai:
    google:
      genai:
        api-key: ${GOOGLE_API_KEY}
        chat:
          options:
            model: gemini-3.6-flash
          response-mime-type: application/json

  datasource:
    url: jdbc:mysql://localhost:3306/product_engine
    username: root
    password: divyalathaprakash@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080